<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= news.title %> - Рейтинг учащихся</title>
    <link rel="stylesheet" href="/css/style.css">
    <style>
        .news-single-container {
            max-width: 1200px;
            margin: 2rem auto;
            padding: 0 2rem;
            display: grid;
            grid-template-columns: 2fr 1fr;
            gap: 3rem;
        }
        
        .news-main {
            background: white;
            border-radius: 20px;
            padding: 2rem;
            box-shadow: 0 5px 15px rgba(0,0,0,0.1);
        }
        
        .news-sidebar {
            background: white;
            border-radius: 20px;
            padding: 1.5rem;
            box-shadow: 0 5px 15px rgba(0,0,0,0.1);
            height: fit-content;
        }
        
        .news-header {
            margin-bottom: 2rem;
        }
        
        .news-title {
            font-size: 2.2rem;
            color: #2c3e50;
            margin-bottom: 1rem;
            line-height: 1.3;
        }
        
        .news-meta {
            display: flex;
            gap: 1.5rem;
            color: #6c757d;
            font-size: 0.9rem;
            margin-bottom: 1rem;
        }
        
        .news-image {
            width: 100%;
            height: 400px;
            object-fit: cover;
            border-radius: 15px;
            margin-bottom: 2rem;
        }
        
        .news-content {
            font-size: 1.1rem;
            line-height: 1.7;
            color: #333;
        }
        
        .news-content p {
            margin-bottom: 1.5rem;
        }
        
        .other-news-title {
            font-size: 1.3rem;
            margin-bottom: 1.5rem;
            color: #2c3e50;
            border-bottom: 2px solid #667eea;
            padding-bottom: 0.5rem;
        }
        
        .other-news-item {
            padding: 1rem;
            border-left: 3px solid #667eea;
            margin-bottom: 1rem;
            background: #f8f9fa;
            border-radius: 5px;
            transition: transform 0.3s;
        }
        
        .other-news-item:hover {
            transform: translateX(5px);
        }
        
        .other-news-item h4 {
            margin: 0 0 0.5rem 0;
            color: #2c3e50;
        }
        
        .other-news-item p {
            margin: 0;
            color: #6c757d;
            font-size: 0.9rem;
        }
        
        .back-button {
            display: inline-block;
            margin-bottom: 2rem;
            padding: 10px 20px;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: white;
            text-decoration: none;
            border-radius: 8px;
            transition: transform 0.3s;
        }
        
        .back-button:hover {
            transform: translateY(-2px);
        }
        
        @media (max-width: 968px) {
            .news-single-container {
                grid-template-columns: 1fr;
            }
            
            .news-title {
                font-size: 1.8rem;
            }
        }
    </style>
</head>
<body>
    <header>
        <nav>
            <div class="logo">
                <img src="/images/logo.footer.jpg" alt="Логотип колледжа" width="60" height="60">
                <span>Рейтинг учащихся</span>
            </div>
            <ul>
                <li><a href="/">Главная</a></li>
                <li><a href="/news">Новости</a></li>
                <li><a href="/rating">Рейтинг</a></li>
                <% if (user) { %>
                    <li><a href="/profile">Личный кабинет</a></li>
                    <% if (user.role === 'admin') { %>
                        <li><a href="/admin">Админ-панель</a></li>
                    <% } %>
                    <li><a href="/auth/logout">Выйти</a></li>
                <% } else { %>
                    <li><a href="/auth/login">Войти</a></li>
                <% } %>
            </ul>
        </nav>
    </header>

    <main>
        <div class="news-single-container">
            <!-- Основной контент новости -->
            <div class="news-main">
                <a href="/news" class="back-button">← Назад к новостям</a>
                <img src="/images/news1.jpg" alt="Добро пожаловать!" class="news-image">
                <div class="news-header">
                    <h1 class="news-title"><%= news.title %></h1>
                    <div class="news-meta">
                        <span>📅 <%= new Date(news.createdAt).toLocaleDateString('ru-RU') %></span>
                        <span>👤 Автор: <%= news.authorName %></span>
                    </div>
                </div>

                <% if (news.image) { %>
                <img src="/uploads/<%= news.image %>" alt="<%= news.title %>" class="news-image">
                <% } else { %>
                <img src="/images/news1.jpg" alt="<%= news.title %>" class="news-image">
                <% } %>

                <div class="news-content">
                    <%- news.content.replace(/\n/g, '<br>') %>
                </div>
            </div>

            <!-- Боковая панель с другими новостями -->
            <div class="news-sidebar">
                <h3 class="other-news-title">📌 Другие новости</h3>
                <% otherNews.forEach(other => { %>
                <div class="other-news-item">
                    <h4><a href="/news/<%= other.id %>" style="color: inherit; text-decoration: none;">
                        <%= other.title %>
                    </a></h4>
                    <p><%= other.content.substring(0, 80) %>...</p>
                    <small>📅 <%= new Date(other.createdAt).toLocaleDateString('ru-RU') %></small>
                </div>
                <% }); %>
                
                <% if (otherNews.length === 0) { %>
                <p>Пока нет других новостей</p>
                <% } %>
            </div>
        </div>
    </main>

    <footer>
        <div class="footer-content">
            <img src="/images/logo-footer.png" alt="Логотип" width="40" height="40">
            <p>&copy; 2025 Система рейтинга учащихся. Все права защищены.</p>
        </div>
    </footer>
</body>
</html>